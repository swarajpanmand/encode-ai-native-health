{
  "name": "health-copilot-extension",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ts": "tsc -p tsconfig.json",
    "build:sidepanel": "cd sidepanel && npm run build",
    "build": "npm run build:ts && npm run build:sidepanel",
    "dev:ts": "tsc -w -p tsconfig.json",
    "dev:sidepanel": "cd sidepanel && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/chrome": "~0.1.32",
    "typescript": "~5.9.3"
  }
}
